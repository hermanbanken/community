<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">{{appname}}</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#if user}}{{#each menuItems}}
        <li class="{{routeClass path}}">
          <a href="{{path}}">{{t title}}</a>
        </li>
        {{/each}}{{/if}}
      </ul>
      <div class="navbar-form navbar-right login">
        <div class="form-group">
          {{#if user}}{{#with user}}
          <a class="btn btn-default" href="/users/{{_id}}"><!--class: member-->
            <!--<img class="member-gravatar" src="{{gravatar profile}}" />-->
            <span class="member-name">{{name profile}}</span>
          </a>
          {{/with}}{{else}}
          <a class='btn btn-default' href="/auth/google">Sign in with Google</a> 
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</nav>